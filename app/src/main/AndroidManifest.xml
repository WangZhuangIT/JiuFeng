<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lingzhuo.jiufeng">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.SearchActivity" />
        <activity android:name=".activity.MsgDetialActivity" />
        <activity android:name=".activity.SearchResultActivity" />
        <activity android:name=".activity.DeadActivity" />
        <activity android:name=".activity.ArticialActivity" />
        <activity android:name=".activity.ActionThemeActivity" />
        <activity android:name=".activity.ThemeWorshipActivity" />
        <activity android:name=".activity.ThemeFlowerActivity" />
        <activity android:name=".activity.ThemeWineActivity" />
        <activity android:name=".activity.ThemeThusActivity" />
        <activity android:name=".activity.Mp3Activity" />
        <activity android:name=".zbar.CameraTestActivity" />
        <activity android:name=".activity.PhotoActivity"></activity>
    </application>

</manifest>
